[project]
name = "imdb-wrapper"
version = "0.1.0"
description = "A Python wrapper for the IMDb GraphQL API"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
